# ---------- Postgres ----------
POSTGRES_DB=markegtingappdb
POSTGRES_USER=admin
POSTGRES_PASSWORD=Passw0rd

# ---------- Django ----------
DJANGO_SECRET_KEY=dev-secret-change-me
DJANGO_DEBUG=1
ALLOWED_HOSTS=localhost,127.0.0.1,backend

# ---------- Next.js ----------
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# PostgreSQL connection for Django (db is the docker service name)
DATABASE_URL=postgres://admin:Passw0rd@db:5432/markegtingappdb

# CORS allowed origins (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://frontend:3000
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://frontend:3000